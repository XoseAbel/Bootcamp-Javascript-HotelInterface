{
  "name": "05_hotel_interface",
  "version": "1.0.0",
  "description": "",
  "main": "./app/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel index.html --out-dir build/debug",
    "build": "parcel build index.html --out-dir build/release"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "typescript": "^3.9.5"
  },
  "dependencies": {
    "app": "^0.1.0"
  }
}
